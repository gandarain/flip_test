const displayName = 'FilterTransaction'
const defaultProps = {
  showFilterModal: false,
  onPressFilterItem: () => { },
  onClose: () => { },
  selectedFilter: null
}

export default { displayName, defaultProps }
